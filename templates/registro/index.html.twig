{% extends 'base.html.twig' %}

{% block title %}Depatro Project{% endblock %}

{% block body %}

{% for flash_message in app.session.flashbag.get('success') %}
    <div class="flash-notice">
        {{ flash_message }}
    </div>
{% endfor %}

<div class="row">
    <div class="container col-xl-4 col-lg-6 col-md-8 col-sm-10 col-xs-12 offset-xl-4  offset-lg-3 offset-md-2 offset-sm-1 offset-xs-0">
        <h1 class="h3 mb-3 font-weight-normal">Registro</h1>
        {{ form(formulario) }}

        O dirijase al <a href="/">Login</a>
    </div>
</div>

{% endblock %}
